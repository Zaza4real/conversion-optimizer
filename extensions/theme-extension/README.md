# Theme App Extension

Blocks: Trust/Guarantee, Shipping & Returns, FAQ, Urgency/Stock, Social proof.
App embed: minimal JS for experiment variant toggles and exposure logging (calls pixel).

Config via block settings (copy, visibility). No raw theme edits; merchant adds blocks to product/page templates.
