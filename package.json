{
  "name": "conversion-optimizer",
  "private": true,
  "scripts": {
    "shopify:link": "pnpm exec shopify app config link",
    "shopify:deploy": "pnpm exec shopify app deploy --allow-updates --allow-deletes",
    "dev:backend": "pnpm --filter backend exec pnpm run start:dev",
    "dev:admin": "pnpm --filter admin exec pnpm run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "railway:build": "pnpm install --frozen-lockfile && pnpm --filter backend run build",
    "railway:start": "pnpm --filter backend run start"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "@shopify/cli": "^3.90.1"
  }
}
