# Nixpacks config when Railway service Root Directory is apps/backend.
# No build phase: committed dist/ artefacts are used directly.
# startCommand in railway.toml runs: node dist/main.js (fast, <1s startup).
[phases.install]
cmds = ["npm ci --ignore-scripts"]
