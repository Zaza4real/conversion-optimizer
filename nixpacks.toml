# Monorepo deploy uses pnpm workspace lock at repo root.
# Build backend via filter to avoid npm lockfile/lifecycle issues.
[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
dependsOn = ["install"]
cmds = ["pnpm --filter backend run build"]

[start]
cmd = "pnpm --filter backend run start"
