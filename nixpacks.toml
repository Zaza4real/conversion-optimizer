# Monorepo-root deploy fallback: run backend install/build directly.
[phases.install]
cmds = ["cd apps/backend && npm ci --ignore-scripts"]

[phases.build]
dependsOn = ["install"]
cmds = ["cd apps/backend && npm run build"]

[start]
cmd = "cd apps/backend && npm run start"
