# Shared

Shared TypeScript types, event names, and validation schemas used by backend and admin (and optionally extensions if we ship shared build). Built with `pnpm build` from repo root or from this package.
